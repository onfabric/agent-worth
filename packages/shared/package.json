{
  "name": "@agent-worth/shared",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "check:types": "bun run --bun tsc -p tsconfig.json --noEmit",
    "typecheck": "bun run check:types"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@agent-worth/typescript-config": "workspace:*",
    "typescript": "catalog:"
  }
}
